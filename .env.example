# Informix connection settings
# Copy this file to .env and fill in the real values
# Or use: cp .env.test .env  (test)  /  cp .env.production .env  (production)

DB_HOSTNAME=host.docker.internal
DB_PORT=9088
DB_USER=informix
DB_PASSWORD=changeme
DB_NAME=testdb
